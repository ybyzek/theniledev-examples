const gaugeGraph = {
  metricName: 'custom.SkyNet.instance.uptime',
  metricTitle: 'Up time',
};

const lineChart = {
  metricName: 'custom.SkyNet.instance.charge',
  metricTitle: 'Defense grid charge',
};

const averageNum = {
  metricName: 'custom.SkyNet.instance.latency',
  metricTitle: 'Average latency (ms)',
};

export { gaugeGraph, lineChart, averageNum };
