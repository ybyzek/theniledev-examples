const gaugeGraph = {
  metricName: 'custom.Workload.instance.uptime',
  metricTitle: 'Up time',
};

const lineChart = {
  metricName: 'custom.Workload.instance.tps',
  metricTitle: 'Tasks per second (TPS)',
};

const averageNum = {
  metricName: 'custom.Workload.instance.latency',
  metricTitle: 'Average process latency (ms)',
};

export { gaugeGraph, lineChart, averageNum };
