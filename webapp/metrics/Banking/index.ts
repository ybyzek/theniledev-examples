const gaugeGraph = {
  metricName: 'custom.Banking.instance.online',
  metricTitle: 'Owner Online',
};

const lineChart = {
  metricName: 'custom.Banking.instance.bps',
  metricTitle: 'Balance Queries per second (BPS)',
};

const averageNum = {
  metricName: 'custom.Banking.instance.payments',
  metricTitle: 'Average payments per sec',
};

export { gaugeGraph, lineChart, averageNum };
